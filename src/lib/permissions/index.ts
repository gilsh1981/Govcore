// Re-export from canonical permissions module
export { requirePermission, PermissionDeniedError, hasPermission, hasAnyPermission } from "@/lib/permissions";
export type { Permission } from "@/lib/permissions";
